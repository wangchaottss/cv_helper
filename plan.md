## 图形化简历制作工具 — 开发计划（第 1 部分）

### 1. 项目概述
构建一个运行于 **macOS** 的桌面端图形化简历制作工具，用户可自由拖拽 **文本单元** 和 **图片单元** 到 A4 画布中，通过鼠标大致定位 + 方向键微调，并借助水平/垂直辅助线实现视觉对齐。制作过程中可存档为 HTML 文件，最终导出为高保真 PDF。

### 2. 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 桌面容器 | **Electron** | 成熟稳定，Node.js + Chromium，macOS 原生体验好；未来可考虑 Tauri 但当前生态更利于快速开发 |
| UI 渲染 | **React 18 + TypeScript** | 组件化、状态管理清晰，社区工具多 |
| 样式 | **Tailwind CSS** 或 CSS Modules | 快速界面搭建 |
| 画布交互 | **原生 Pointer Events + 自定义 hooks** | 无需重型库，完全可控 |
| 富文本 | **contentEditable + 基础格式化命令** | 避免引入大型编辑器，后期可按需集成 Slate.js |
| PDF 生成 | **html2canvas + jsPDF**（前端方案） | 或使用 Electron 内建 `BrowserWindow` 渲染再打印为 PDF |
| 打包分发 | **electron-builder** | 可生成 macOS `.dmg` |

> **存档格式推荐**：采用 **单个 HTML 文件**，其中包含完整的内联样式、字体声明和画布状态（通过 JSON 嵌入 `script` 标签），双击即可在浏览器中还原，同时易于程序加载恢复。比纯 JSON + 外部资源更自包含，分发友好。

### 3. 功能需求

- **元素类型**：文本框（可编辑文字）、图片框（支持常见格式）
- **拖放创建**：从素材面板拖拽至画布任意位置，松开即创建
- **移动与微调**：鼠标拖动调整位置，方向键以 1px / 10px（加 Shift）微调
- **辅助线**：元素移动时自动吸附并显示水平 / 垂直对齐线（相对其他元素或画布中线），导出时不渲染
- **选中与多选**：单击选中，Shift / Cmd 多选，支持批量移动
- **属性编辑**：选中元素后右侧面板可修改字体、字号、颜色、加粗、倾斜、对齐方式；图片可替换、裁剪、圆角
- **画布设置**：A4 尺寸（210×297mm），可缩放视图，可显示/隐藏辅助线
- **撤销/重做**：完整的操作历史
- **保存/加载**：一键保存为 HTML 存档，支持从 HTML 恢复
- **导出 PDF**：按 A4 尺寸生成单页或多页 PDF，辅助线不出现

### 4. 系统架构

```
┌────────────────────────────────────────┐
│             Electron Main              │
│  - 窗口管理                             │
│  - 原生菜单（打开/保存/导出）            │
│  - 文件系统访问                         │
└──────────────┬─────────────────────────┘
               │ IPC
┌──────────────▼─────────────────────────┐
│          Renderer (React)              │
│                                        │
│  ┌───────────┐  ┌──────┐  ┌────────┐  │
│  │ 组件面板   │  │A4画布│  │属性面板 │  │
│  │(文本/图片) │  │      │  │(字体等)│  │
│  └───────────┘  └──────┘  └────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │        状态管理 (Zustand)         │  │
│  │  - 元素列表                       │  │
│  │  - 选区状态                       │  │
│  │  - 历史记录                       │  │
│  │  - 辅助线数据                     │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │          服务层                   │  │
│  │  - 对齐计算                       │  │
│  │  - 序列化/反序列化                │  │
│  │  - PDF导出                       │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

### 5. 核心模块设计（一）

#### 5.1 画布模块（Canvas）
- **A4 画布容器**：使用 `div` 模拟，CSS 设置固定宽高比（794px × 1123px 对应 96dpi），居中显示，背景白色，可缩放（通过 `transform: scale()` 配合状态）。
- **坐标系统**：所有元素相对于画布左上角绝对定位（`position: absolute; left/top`），统一单位为像素（px），内部存储和序列化时保留像素值，导出 PDF 时按 96dpi 换算为毫米。
- **交互层**：覆盖整个画布的透明 `div` 用于捕获鼠标事件（拖拽新元素、移动已有元素、绘制选区框）。

#### 5.2 拖拽与定位
- **素材拖入**：素材面板中的文本/图片模板设置 `draggable`，画布监听 `drop` 事件，根据落点坐标和模板尺寸创建新元素。
- **元素移动**：
  - 鼠标按下（onPointerDown）选中元素，移动时实时更新元素坐标，触发对齐检测。
  - 方向键微调：监听键盘事件，当有选中元素时，按方向键 ±1px，Shift + 方向键 ±10px。
  - 坐标范围限制：元素不能完全拖出画布，至少保留一部分可见。
- **性能优化**：移动过程中使用 `requestAnimationFrame` 批量更新，避免频繁触发重排。

#### 5.3 元素管理
- **元素数据结构**（TypeScript 接口）：
```typescript
interface BaseElement {
  id: string;
  type: 'text' | 'image';
  x: number;
  y: number;
  width: number;
  height: number;
  rotation: number;        // 暂可先为0，预留
  zIndex: number;
}

interface TextElement extends BaseElement {
  type: 'text';
  content: string;
  fontSize: number;
  fontFamily: string;
  fontWeight: number;
  fontStyle: 'normal' | 'italic';
  textAlign: 'left' | 'center' | 'right';
  color: string;
  backgroundColor: string;
  lineHeight: number;
}

interface ImageElement extends BaseElement {
  type: 'image';
  src: string;             // base64 或文件路径（存档时转为 base64）
  objectFit: 'fill' | 'contain' | 'cover';
}
```
- **渲染**：根据类型渲染对应的 DOM 节点，文本单元为 `div`（contentEditable），图片单元为 `img` 或带背景的 `div`。
- **选中态**：加蓝色边框和八个控制点（四角和四边中点），控制点可拖拽缩放（后期迭代）。

### 5. 核心模块设计（二）

#### 5.4 辅助线系统
- **对齐检测**：在元素移动过程中，实时计算当前元素边缘/中心与其他元素边缘/中心以及画布中线的水平/垂直距离。当距离小于阈值（如 5px），自动吸附并将当前坐标修正到对齐位置。
- **辅助线渲染**：
  - 使用独立的 SVG 层或 `div` 元素（绝对定位，覆盖画布），绘制水平线和垂直线。
  - 线条样式：半透明蓝色或红色虚线，线宽 1px。
  - 只在元素移动且发生吸附时显示。
  - 线条位置根据对齐对象的坐标实时更新。
  - 辅助线元素不属于画布内容，导出/保存时自动排除。
- **吸附规则**：
  - 水平对齐：左边缘、右边缘、水平中心。
  - 垂直对齐：上边缘、下边缘、垂直中心。
  - 画布中心线（水平/垂直中线）始终作为参考对象。
  - 多选元素整体移动时以选区包围盒计算对齐。
- **可配置性**：在工具栏提供开关，允许用户暂时禁用吸附或隐藏辅助线。

#### 5.5 属性面板
- **联动逻辑**：当用户选中元素（单选），右侧面板显示对应的属性编辑控件，修改属性即时更新画布中的元素。
- **文本属性**：字体、字号、颜色（取色器）、加粗/倾斜按钮、对齐方式、行高、背景色。
- **图片属性**：图片替换按钮（触发文件选择），适配方式（填充/适应/拉伸），圆角半径，透明度。
- **通用属性**：位置（X, Y 数字输入框）、尺寸（宽、高，可锁定比例）、旋转角度（后期）。
- **多选时**：部分属性可能不支持批量修改（如文本内容），仅显示可批量修改的属性（位置、尺寸、字体等），或将文本内容输入框置灰。本阶段优先实现单选编辑，多选时属性面板显示“混合”或隐藏文本编辑。

#### 5.6 状态管理（Zustand）
- **Store 结构**：
  ```typescript
  interface EditorState {
    elements: Record<string, BaseElement>;  // 所有元素
    selection: string[];                    // 选中元素 ID 列表
    zoom: number;                           // 画布缩放比例，1=100%
    showGuides: boolean;                    // 辅助线开关
    snapEnabled: boolean;                   // 吸附开关
    guideLines: { horizontal: number[]; vertical: number[] }; // 当前显示的辅助线位置

    // Actions
    addElement: (element: BaseElement) => void;
    updateElement: (id: string, patch: Partial<BaseElement>) => void;
    removeElements: (ids: string[]) => void;
    setSelection: (ids: string[]) => void;
    setZoom: (zoom: number) => void;
    // 撤销/重做
    undo: () => void;
    redo: () => void;
  }
  ```
- **历史记录**：基于命令模式或快照差异。简单方案：维护一个 `history` 栈和 `future` 栈，每个操作前将当前状态快照（`elements` 的深拷贝）压入 `history`。撤销时从 `history` 弹出恢复到 `elements`，并将当前状态压入 `future`。性能考虑：快照可只针对变化的元素做增量记录，但初期可接受全量快照。

#### 5.7 保存与加载（HTML 存档）
- **保存流程**：
  1. 从 Store 获取所有元素数据。
  2. 构建一个独立 HTML 文档字符串，包含：
     - 所有元素的内联样式表示（绝对定位的 div）。
     - 图片数据转为 base64 嵌入 `src`（保证离线可用）。
     - 当前画布缩放等元数据可嵌入 JSON 脚本标签（用于恢复编辑），或忽略（仅展示用）。
  3. 使用 Electron 的 `dialog.showSaveDialog` 让用户选择路径，然后通过 `fs.writeFile` 写入。
- **加载流程**：
  1. `dialog.showOpenDialog` 选择 HTML 文件。
  2. 读取文件内容，解析 DOM 或提取 JSON 元数据。
  3. 根据元素信息恢复 Store 状态，清空画布并渲染。
  4. 兼容性：如果加载的是纯展示 HTML（不含元数据），则提示用户或仅作为图片查看；我们的工具保存时统一包含元数据。
- **更优存档格式讨论**：HTML 自包含的优点明显，但若需要跨应用编辑，可考虑同时导出 `.json` 配置文件。目前保持单一 HTML 作为主存档格式，满足“中途储存”和快速分享需求。

#### 5.8 PDF 导出
- **方案 A（推荐）**：利用 Electron 的 `BrowserWindow.webContents.printToPDF`。
  - 步骤：
    1. 在当前渲染进程或新建一个隐藏的 `BrowserWindow` 中，渲染一个仅包含画布内容（无辅助线、无 UI）的完整 HTML 页面，页面尺寸设为 A4（CSS `@page { size: A4; margin: 0; }`）。
    2. 调用 `webContents.printToPDF({ pageSize: 'A4', marginsType: 0 })` 生成 PDF Buffer。
    3. 弹出保存对话框写入文件。
  - 优势：原生 PDF 质量高，支持字体嵌入（系统字体），无水印，无额外依赖，与浏览器打印效果一致。
- **方案 B（备选）**：前端方案 `html2canvas` + `jsPDF`。
  - 若不用 Electron 主进程能力，可在渲染进程直接截图画布生成图片再合成 PDF。但文字清晰度可能受限，且不支持文本选择/搜索。可作为纯 Web 版本的备选，在 Electron 中优先使用方案 A。
- **多页处理**：画布高度固定为 A4（297mm），当用户需要多页简历时，应考虑支持添加多个画布页面。初期可限制为单页，后续扩展多页面板。

### 6. UI/UX 布局

```
┌──────────────────────────────────────────────────────┐
│  菜单栏 (文件/编辑/查看/帮助)                        │
├───────────┬────────────────────────────┬──────────────┤
│  素材面板  │        A4 画布区域         │   属性面板    │
│           │                            │              │
│ [文本] 拖  │  ┌────────────────────┐   │ 字体: ___    │
│ [图片] 拖  │  │                    │   │ 大小: ___    │
│           │  │   A4 画布 (可缩放)  │   │ 颜色: [■]   │
│           │  │                    │   │ B / I / U   │
│           │  │   - 元素1          │   │              │
│           │  │   - 元素2          │   │ 位置 X:__    │
│           │  │                    │   │      Y:__    │
│           │  └────────────────────┘   │ 宽高:__×__   │
│           │                            │              │
├───────────┴────────────────────────────┴──────────────┤
│  状态栏 (缩放:100% | 辅助线:开/关 | 吸附:开/关)      │
└──────────────────────────────────────────────────────┘
```

- **素材面板**：拖拽源提供文本块和图片占位符，可配置默认样式。
- **画布区域**：居中显示 A4 纸效果，支持鼠标滚轮缩放，按住空格+拖拽平移（当画布大于可视区域时）。
- **属性面板**：折叠/展开分组，占用右侧固定宽度。
- **状态栏**：快速切换辅助线和吸附。

### 7. 开发阶段规划

| 阶段 | 内容 | 预计产出 |
|------|------|----------|
| **P0 基础骨架** | Electron + React 项目搭建；三栏布局；空白 A4 画布（固定尺寸，可缩放） | 可运行的空编辑器 |
| **P1 元素添加与移动** | 素材面板拖放产生文本/图片单元；鼠标拖动移动元素；方向键微调（1px/10px） | 可自由放置元素 |
| **P2 属性编辑** | 单击选中元素，属性面板联动；修改字体、颜色、大小等并实时更新 | 单个元素完整编辑能力 |
| **P3 辅助线系统** | 移动时检测对齐、显示辅助线、吸附功能；开关控制；辅助线不在导出中显示 | 对齐辅助体验就绪 |
| **P4 多选与操作** | Shift/Cmd 多选；批量移动；删除选中元素；基础撤销/重做（全量快照） | 基本编辑闭环 |
| **P5 存档与加载** | 保存为自包含 HTML（含内联样式和 base64 图片）；从 HTML 恢复编辑状态 | 可保存可恢复 |
| **P6 PDF 导出** | 使用 Electron 原生打印为 PDF；隐藏辅助线和 UI；正确 A4 输出 | 核心产物达成 |
| **P7 细节打磨** | 键盘快捷键（复制/粘贴/全选）；画布网格背景；图片替换与裁剪；界面美化 | 可发布原型 |
| **P8 未来扩展** | 多页支持、模板系统、文本块富文本增强（列表/链接）、撤销优化（增量记录）、字体包嵌入 | 产品化 |

### 8. 关键技术难点及对策

#### 8.1 辅助线渲染与吸附属实
- **挑战**：移动过程中需要高频（每帧）计算所有元素的对齐关系，若元素数量较多可能卡顿。
- **对策**：
  - 使用空间索引（如简单的四叉树）或只对当前移动元素的边界与周围元素做碰撞检测，而非全量遍历。初期元素数量少时无需过度优化。
  - 移动过程中只更新辅助线 DOM，不修改其他元素，减少重排。

#### 8.2 图片元素的存档和 PDF
- **挑战**：用户可能添加本地图片，保存的 HTML 若使用文件路径则无法分享，必须嵌入图片数据。
- **对策**：
  - 添加图片时直接读取文件生成 DataURL（base64）存入元素 `src`。注意大图片限制尺寸，前端可做 resize 处理（Canvas 压缩）。
  - 保存 HTML 时图片已在元素中，无需额外处理。
- **PDF 挑战**：`printToPDF` 能正确处理 DataURL 图片，但若图片过大可能导致生成的 PDF 很大。可在导出前提供图片压缩选项。

#### 8.3 文本编辑体验
- **挑战**：直接使用 `contentEditable` 存在浏览器兼容性和格式不一致问题。
- **对策**：
  - 初期限制文本块为纯文本，属性面板控制整体样式，简化编辑逻辑。
  - 若需粗体/部分文字不同样式，可后续集成轻量富文本库如 **TipTap** 或 **Quill**，但会引入复杂度。本工具以简历排版为目的，整体统一格式通常已足够，初期保持简单。

#### 8.4 缩放与坐标换算
- **挑战**：画布缩放时，元素的实际像素位置和显示位置需要换算。
- **对策**：
  - 状态中存储的坐标始终是**在 100% 缩放下的画布逻辑坐标**。
  - 渲染时通过 CSS `transform: scale(${zoom})` 作用在画布容器上，所有元素自动跟随。
  - 鼠标事件获取的坐标需要除以当前缩放比转换回逻辑坐标，再更新状态。
  - 方向键微调直接在逻辑坐标上加减，显示正确。

#### 8.5 多平台兼容性（当前仅 macOS）
- 虽然目标为 macOS，但代码应尽量跨平台友好，Electron 本身支持 Windows/Linux，避免使用 macOS 特有 API（如 `NSWorkspace`），保持可移植性。

### 9. 工具配置与开发环境

- **包管理器**：推荐 `pnpm` 或 `npm`
- **构建工具**：`Vite` 用于 React，配合 `electron-vite` 统一 Electron 主进程和渲染进程构建
- **代码规范**：ESLint + Prettier，TypeScript 严格模式
- **测试策略**：
  - 单元测试：工具函数（对齐算法、序列化、坐标转换）用 Vitest
  - 组件测试：React Testing Library 测试关键交互
  - E2E 测试（后期）：Playwright + Electron 可模拟拖放和导出验证

### 10. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| `printToPDF` 无法完全复制 CSS 效果（阴影/滤镜等） | PDF 保真度下降 | 设计时避免复杂 CSS 效果，导出前转换为通用属性 |
| 保存的 HTML 过大（图片多） | 存档加载慢 | 图片自动压缩到合理分辨率（300dpi 打印足够），提示用户分辨率 |
| 辅助线吸附逻辑复杂，开发周期长 | 影响里程碑 | 先实现基础吸附（边缘/中心/画布中线），不对齐所有元素的组合 |
| Electron 打包体积大 | 分发不利 | 目前可接受，后续可评估 Tauri 迁移 |
| 用户拖拽图片时文件过大导致主进程卡顿 | 体验差 | 在渲染进程用 Canvas 缩放图片后再转为 dataURL，异步处理 |

### 11. 总结

本开发计划覆盖了一个**macOS 桌面简历制作工具**从架构到实现的全流程。在 Claude Code 的协助下，可将每个开发阶段拆分为具体任务，逐步实现拖放编辑、对齐辅助、HTML 存档和 PDF 导出的完整闭环。

下一步可立即开始 **P0 基础骨架**的搭建：使用 `electron-vite` 初始化项目，创建 React 主界面和三栏布局，并在画布区域渲染一个符合 A4 比例的白色容器。
